<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <!--
      Launch screen. ONE stage, on every Android version.

      This used to show two: Android 12+ always draws its own launch screen
      with the app icon, and then this theme's old full-screen splash.png
      (the Capacitor template default) took over with the same hexagon at a
      much smaller size until the site loaded. Big icon, small icon, app.

      Now there is only the system screen, fed our teal and our icon, and
      MainActivity holds it until the page has actually loaded. The icon is
      the launcher foreground, so it is the exact size the system used
      anyway. core-splashscreen draws the same thing on Android 7 to 11.
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>